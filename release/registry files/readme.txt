To add printing commands to right click context menu:
In shellextension.reg replace <path-to-app> with the full path to main.exe (app executable)
Then import the registry keys
Registry backup recommended beforehand