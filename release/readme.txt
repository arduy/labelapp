Installation:
Run makescripts.exe (must be in the same folder as main.exe)
Resulting batch scripts can be moved (and copied) anywhere, but leave everything else as is
Run batch scripts by dragging and dropping your data file onto them
Edit config.ini to include your printer name
Printer name will either be the local name of your printer, or if being accessed on a network, the full network address